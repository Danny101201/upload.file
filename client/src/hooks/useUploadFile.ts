import { useMutation } from "@tanstack/react-query";
const postResetPasswordBatchByFile = async (body:) => { }
export const useUploadFile = () => useMutation({
  mutationKey: ['useUploadFile'],
  mutationFn: postResetPasswordBatchByFile
})